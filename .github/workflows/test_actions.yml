name: Test GitHub Secrets
on: 
  workflow_dispatch: # This adds a "Run" button in the GitHub UI

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Run Test Script
        env:
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY_GITHUB }}
          GCP_SERVICE_ACCOUNT_JSON: ${{ secrets.SERVICE_ACCOUNT_KEY_GITHUB }}
        run: python utils/utils_test_connection.py